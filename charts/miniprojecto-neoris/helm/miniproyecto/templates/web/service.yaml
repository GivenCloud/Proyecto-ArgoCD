apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  type: NodePort
  selector:
    app: web-server
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.web.port }}
      nodePort: {{ .Values.web.nodePort }}
