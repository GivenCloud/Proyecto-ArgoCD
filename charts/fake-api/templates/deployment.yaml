apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fake-api.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "fake-api.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "fake-api.name" . }}
    spec:
      containers:
        - name: fake-api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80